//OnlineCardDepositsLoginContainer.jsx

import React, {PropTypes} from 'react'
import compose from '../utils/compose'
import partial from '../utils/partial'
import OnlineCardDepositsLogin from '../components/deposits/OnlineCardDepositsLogin'

export default function OnlineCardDepositsLoginContainer({state, dispatch, id}) {
  const props = {}

  return <OnlineCardDepositsLogin />; 
}