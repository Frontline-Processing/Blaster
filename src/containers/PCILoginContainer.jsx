//PCILoginContainer.jsx

import React, {PropTypes} from 'react'
import compose from '../utils/compose'
import partial from '../utils/partial'
import PCILogin from '../components/pci/PCILogin'

export default function PCILoginContainer({state, dispatch, id}) {
  const props = {}

  return <PCILogin />; 
}